# Generated by: Metadata Agent (Phase 4a)  |  template: schedule.v1
workload: product_inventory

schedule:
  engine: eventbridge_scheduler
  cron: "cron(0 8 * * ? *)"
  timezone: UTC
  flexible_time_window: "OFF"

execution:
  state_machine: product_inventory_pipeline
  retries: 2
  backoff: exponential
  backoff_rate: 2.0
  interval_seconds: 30
  timeout_minutes: 60

failure_handling:
  on_failure: notify
  sns_topic: product-inventory-alerts
  quarantine_review_required: false

dependencies: []
