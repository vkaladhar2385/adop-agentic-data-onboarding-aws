# Generated by: Metadata Agent (Phase 4a)  |  template: semantic.v1
workload: product_inventory
namespace: catalog

entities:
  - name: ProductInventory
    grain: one current snapshot row per sku
    primary_key: sku

columns:
  sku:            { role: identifier, type: string,  pii: false }
  product_name:   { role: attribute,  type: string,  pii: false }
  category:       { role: dimension,  type: string,  pii: false }
  warehouse_id:   { role: dimension,  type: string,  pii: false }
  on_hand_qty:    { role: measure,    type: integer, pii: false }
  reserved_qty:   { role: measure,    type: integer, pii: false }
  unit_cost:      { role: measure,    type: decimal, pii: false }
  list_price:     { role: measure,    type: decimal, pii: false }
  supplier_id:    { role: dimension,  type: string,  pii: false }
  supplier_name:  { role: attribute,  type: string,  pii: false, note: business name, not PII }
  status:         { role: dimension,  type: string,  pii: false }
  updated_at:     { role: temporal,   type: timestamp, pii: false }
  ingestion_date: { role: temporal,   type: date,    pii: false }
  available_qty:  { role: measure,    type: integer, pii: false, derived: true }
  inventory_value:{ role: measure,    type: decimal, pii: false, derived: true }
  margin_pct:     { role: measure,    type: decimal, pii: false, derived: true }

lf_tags: []   # no PII columns — register_catalog applies no column tags

business_terms:
  available_qty: "on_hand_qty minus reserved_qty (may be negative if over-reserved)."
  inventory_value: "on_hand_qty times unit_cost."
  margin_pct: "list_price minus unit_cost, divided by list_price."
