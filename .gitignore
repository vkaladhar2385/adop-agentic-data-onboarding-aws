# Local pipeline outputs (regenerated by run_local_pipeline.py)
output/

# Generated sample data (regenerated by the data generator)
demo/sample_data/*.csv

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Terraform (never commit live state — it contains resource IDs)
iac/terraform/.terraform/
iac/terraform/*.tfstate*
iac/terraform/terraform.tfvars
iac/terraform/tfplan
iac/terraform/*.tfplan

# Regenerated Lambda packaging (redis-py tree is huge)
build/

# Cloned upstream repos — local study only, not this project's source
reference/

# Local scratch / CloudWatch dumps
fail*.txt
exec*.txt
gold_quality*.txt
glue_error*.txt
sfn_fail.txt
verifier_log.txt
*.jsonl

# IDE
.pytest_cache/
